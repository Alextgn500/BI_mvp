"""
Конфигурация Django‑приложения app_kpis.

Этот модуль содержит класс AppKpisConfig, наследующий django.apps.AppConfig,
который задаёт метаданные приложения app_kpis (имя пакета и поле default_auto_field).
Используется Django при загрузке приложений и при регистрации моделей для миграций.
"""

from django.apps import AppConfig


class AppKpisConfig(AppConfig):
    """
    Конфигурация приложения app_kpis.

    Атрибуты:
    - name: полное имя модуля приложения, используемое Django для импорта (по умолчанию 'app_kpis').
    - default_auto_field: тип поля по умолчанию для автоматически создаваемых первичных ключей.

    Этот класс позволяет явно задать конфигурацию приложения. Если понадобится указать
    пользовательскую метку приложения (label) или выполнить дополнительную инициализацию
    при старте (в методе ready), добавляйте соответствующие поля/методы здесь.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "app_kpis"
